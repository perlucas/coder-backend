const PI = Math.PI

class Equation {
    solve() {
        return 0
    }
}

module.exports = {

    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,

    PI,

    Equation
}